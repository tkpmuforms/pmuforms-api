export enum UserRole {
  ARTIST,
  CUSTOMER,
}

export enum FilledFormStatus {
  COMPLETED = 'completed',
  INCOMPLETE = 'incomplete',
  SIGNED = 'signed',
}

export enum ReminderType {
  CHECK_IN = 'check-in',
  FOLLOW_UP = 'follow-up',
}
